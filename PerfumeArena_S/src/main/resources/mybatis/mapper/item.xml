<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="item">

	<!-- 메인페이지 카테고리 리스트 --> 
	<select id="categoryList" resultType="CategoryDTO">
		SELECT categoryNo, categoryName, categoryImagePath
		FROM category
		ORDER BY categoryNo
	</select>
	
	<!-- 제품 등록하기 -->
	<insert id="itemRegister" parameterType="ItemDTO">
		INSERT INTO item(itemNo, itemName, fk_category_no, company, itemPhotoPath, 
						 infoImg , itemInfo, itemAmount, price, volume)
	    VALUES(item_seq.nextval, #{itemName}, #{fk_category_no}, #{company}, #{itemPhotoPath}, 
	    	   #{infoImg}, #{itemInfo}, #{itemAmount}, #{price}, #{volume})
	</insert>

</mapper>